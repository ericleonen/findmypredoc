# service

Placeholder for the scheduled job (e.g. an AWS Lambda/cron) that runs `findmypredoc`'s
scraping pipeline on a recurring basis (daily) to refresh the listings database. Not yet
implemented — exact hosting/trigger mechanism (AWS Lambda, ECS scheduled task, etc.) is
still undecided.