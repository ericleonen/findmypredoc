[project]
name = "findmypredoc-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.117.1",
    "psycopg[binary]",
    "psycopg-pool",
    "python-dotenv",
]

[tool.vercel]
entrypoint = "app.server:app"
